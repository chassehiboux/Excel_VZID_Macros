# Структура проекта: новая надстройка VZID

## Статус

Рабочий документ для старта реализации MVP.

## Что это даст в работе

В репозитории сразу будет понятное разделение:

- где лежит основная бизнес-надстройка;
- где лежит код безопасного обновления;
- где лежат шаблоны конфига и релиза;
- где лежит документация для сборки и выпуска.

Это нужно, чтобы дальше не собирать проект из случайных кусков и чтобы публикация новой версии была предсказуемой.

## Базовое решение по репозиторию

- Репозиторий один: `https://github.com/chassehiboux/Excel_VZID_Macros`
- Он же хранит исходники, документацию, установщик и GitHub Releases.
- `MainVZID` проверяет обновления по `manifest.json` из `latest release`.

Базовый URL для MVP:

- `https://github.com/chassehiboux/Excel_VZID_Macros/releases/latest/download/manifest.json`

## Структура каталогов

```text
docs/new-vzid-addin/
installer/
updater/
release/
config/
scripts/
src/main-vzid/
src/main-vzid/modules/
src/main-vzid/workbook/
src/main-vzid/customui/
src/main-vzid/forms/
```

## Зоны ответственности

### `installer/`

Содержит описание состава `setup.exe` и правила первой установки без прав администратора.

Технический подход для MVP:

- `setup.exe` собирается через `PyInstaller`;
- сама логика установки написана на Python;
- это позволяет собирать продукт без системного setup-компилятора и без прав администратора;
- регистрация `MainVZID.xlam` делается через пользовательские ключи Excel, а не через `XLSTART`.

### `updater/`

Содержит код маленького `updater.exe`.

Задача `updater.exe` в MVP:

- дождаться полного закрытия Excel;
- проверить скачанный файл обновления;
- сделать резервную копию текущего `MainVZID.xlam`;
- заменить рабочий файл;
- записать результат в лог;
- при необходимости заново открыть Excel.

Ограничение MVP:

- `updater.exe` не обновляет сам себя;
- новая версия `updater.exe` ставится через новый `setup.exe`.

### `release/`

Содержит договорённость по именам файлов релиза и параметрам `manifest.json`.

### `config/`

Содержит шаблон пользовательского `config.json`, который установщик кладёт в профиль пользователя.

### `src/main-vzid/`

Содержит исходники основной надстройки.

Задача основной надстройки в MVP:

- показать вкладку `VZID`;
- отрисовать dropdown выбора региона;
- показать общий набор кнопок;
- отключать недоступные команды;
- маршрутизировать кнопку в нужный макрос по выбранному региону;
- показывать статус обновления и сервисные действия;
- скачивать релиз и запускать локальный `updater.exe`.

## Модули основной надстройки

- `MainConfig.bas`
  чтение и запись общего `config.json`.
- `MainEntryPoint.bas`
  общий вход и сервисные команды.
- `RibbonVZID.bas`
  callbacks ленты Excel.
- `RegionState.bas`
  текущий регион и его сохранение.
- `CommandRegistry.bas`
  список бизнес-команд и их привязка к макросам по регионам.
- `CommandDispatcher.bas`
  запуск нужного макроса по выбранной кнопке.
- `AccessPolicy.bas`
  права по Windows-логину.
- `MainUpdates.bas`
  проверка `manifest.json`, скачивание файла обновления и подготовка запуска `updater.exe`.
- `UpdaterBridge.bas`
  безопасный запуск локального `updater.exe` с понятными параметрами.
- `UpdateStatus.bas`
  чтение статуса обновления для отображения на вкладке.

## Компоненты updater

- `updater_main.py`
  точка входа `updater.exe`.
- `wait_excel.py`
  ожидание полного закрытия процессов Excel.
- `swap_files.py`
  резервная копия и замена `MainVZID.xlam`.
- `updater_log.py`
  запись результата обновления.

Имена модулей пока ориентировочные и могут быть уточнены при реализации.

## Состав релиза MVP

Файлы, которые должны лежать в GitHub Release:

- `MainVZID.xlam`
- `manifest.json`
- `setup.exe`

Локально при сборке также появляется служебный `updater.exe`, который входит в состав установки.

## Договорённость по manifest

`manifest.json` в MVP должен описывать:

- версию релиза;
- дату публикации;
- URL скачивания `MainVZID.xlam`;
- `sha256` файла `MainVZID.xlam`;
- минимальную поддерживаемую версию `updater.exe`;
- ссылку на заметки к релизу.

## Договорённость по config

`config.json` в MVP должен хранить:

- выбранный регион;
- текущую активную версию `MainVZID`;
- путь к подготовленному обновлению;
- время последней проверки обновлений;
- статус последней проверки;
- правила доступа по логину;
- логин `dzuikevich` с полным доступом по умолчанию.

Техническое упрощение MVP:

- пока используем плоские строковые поля JSON;
- права доступа по командам храним в `...Csv`-полях;
- это упрощает первый стабильный прототип без тяжёлого JSON-парсера в VBA.

## Что делаем следующим шагом

1. Импортировать этот каркас в тестовый `MainVZID.xlam`.
2. Привязать к новой ленте реальные макросы и оставшиеся рабочие `UserForm`.
3. Доделать скачивание релиза и проверку `sha256`.
4. Реализовать локальный `updater.exe`, который ждёт закрытия Excel и меняет файл.
5. Собрать первый пользовательский `setup.exe`.
